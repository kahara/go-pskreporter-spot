name: Image

on: [push]

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v3

      - name: Build
        run:
          echo "Foo!"

  push:
    needs:
      - test
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v3

      - name: Push
        run:
          echo "Foo!"
